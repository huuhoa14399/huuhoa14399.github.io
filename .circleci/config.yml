# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/node:8.11.1

      # Specify service dependencies here if necessary
      # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/mongo:3.4.4

    steps:
      - checkout
      - run:
          name: Deploying to GitHub Pages
          command: |
            git config --global user.email "hoanguyenhuu143@gmail.com"
            git config --global user.name "huuhoa14399"
            echo "machine github.com login huuhoa14399 password $GITHUB_TOKEN" > ~/.netrc
            cd website && yarn install && GIT_USER=huuhoa14399 USE_SSH=true yarn run publish-gh-pages